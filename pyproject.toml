[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "turkish-fin-bert"
version = "0.1.0"
description = "BIST hisseleri için Türkçe finansal duygu analizi ve basit sinyal/backtest hattı."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "joblib>=1.3",
    "matplotlib>=3.8",
    "numpy>=1.26",
    "pandas>=2.1",
    "scikit-learn>=1.4",
    "seaborn>=0.13",
    "yfinance>=0.2",
]

[project.optional-dependencies]
nlp = [
    "torch>=2.2",
    "transformers>=4.40",
]
portfolio = [
    "PyPortfolioOpt>=1.5",
]
dev = [
    "pytest>=8.0",
]

[project.scripts]
fetch_data = "turkish_fin_bert.fetch_data:main"
fetch_news = "turkish_fin_bert.fetch_news:main"
audit_aliases = "turkish_fin_bert.audit_aliases:main"
create_labeling_batch = "turkish_fin_bert.create_labeling_batch:main"
auto_label_batch = "turkish_fin_bert.auto_label:main"
mine_negative_examples = "turkish_fin_bert.mine_negative_examples:main"
mine_positive_examples = "turkish_fin_bert.mine_positive_examples:main"
merge_labels = "turkish_fin_bert.merge_labels:main"
prepare_dataset = "turkish_fin_bert.prepare_dataset:main"
train_model = "turkish_fin_bert.train_model:main"
train_transformer = "turkish_fin_bert.train_transformer:main"
score_news = "turkish_fin_bert.score_news:main"
daily_alerts = "turkish_fin_bert.daily_alerts:main"
analyze_financial_effect = "turkish_fin_bert.financial_effect:main"
backtest = "turkish_fin_bert.backtest:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
